{% include 'breadcrumb' %}

<div class="grid">

  <div class="grid-item large--two-thirds push--large--one-sixth">
      {% unless page.handle == "player-template" %}
    <h1>{{ page.title }}</h1>
      {% endunless %}
    <div class="rte">
      {{ page.content }}
    </div>

  </div>

</div>
